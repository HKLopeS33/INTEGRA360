[build]
  command = "npm install && npm run build --workspace @sistema/desktop"
  publish = "apps/desktop/dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
